import { Plan } from '../core/agentic/types';

export class CoderService {
  async code(plan: Plan): Promise<string> {
    // Placeholder implementation
    // Returns a diff string
    return '';
  }
}